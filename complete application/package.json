{
  "name": "blocklearner",
  "version": "1.0.0",
  "description": "A MERN stack application for learning",
  "scripts": {
    "install-all": "cd frontend && npm install --legacy-peer-deps && cd ../backend && npm install",
    "build": "cd frontend && npm run build && cd ../backend && mkdir -p public && cp -r ../frontend/dist/* public/",
    "start": "cd backend && node index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ]
}
